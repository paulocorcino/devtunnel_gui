# Rust
/target/
**/*.rs.bk
*.pdb

# Slint
*.slint.cache

# Build / dist
/dist/
*.exe~

# App runtime data (local config/cache lives in %APPDATA%, not the repo)
*.log

# OS
Thumbs.db
Desktop.ini

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Claude / agents scratch (issues are tracked under docs/backlogs/, not here)
.scratch/
.claude/

# Ralph autonomous loop (now a global tool at ~\ralph): in-place run scratch + logs
.ralph/

# Staged-plan gate logs (local evidence, not versioned)
docs/plans/logs/
__pycache__/
# Ralphy run artifacts (scratch, logs, per-run plans)
.ralphy/

# Profiling artifacts (samply captures + symbol sidecars) — large binaries,
# kept locally for analysis only, never versioned.
profile*.json.gz
*.json.syms.json
